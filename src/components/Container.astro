---
interface Props {
  class?: string;
  id?: string;
}
const { class: className = "", id: idContainer } = Astro.props;
---

<div class={`max-w-6xl w-full mx-auto px-4 ${className}`} id={idContainer}>
  <slot />
</div>
